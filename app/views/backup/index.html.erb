<div class="header">
  <h1 class="col-md-6 col-md-offset-2">Backup</h1>
</div>


<div class="belowHeader">
  <h2>Back up all clients to excel workbook:</h2>

  <br>
  <p>Please enter both start and end dates for filtering:</p>
  <%= form_tag({}, {class: 'form-inline', method: 'get'}) do %>
      <%= text_field_tag :start_date, params[:start_date], class: 'form-control', placeholder: 'Start Date' %>
      <%= text_field_tag :end_date, params[:end_date], class: 'form-control', placeholder: 'End Date' %>
      <%= submit_tag 'Search', name: nil  %>
  <% end %>
  <br>

    <% if !params[:start_date].to_s.empty? && !params[:end_date].to_s.empty? %>

      <p> PLEASE READ CAREFULLY</p>
      <p> On Clicking below link backup of your data would be sent to your registered email address as a zip file</p>
      <p> You have chosen dates between : <%=params[:start_date]%> and <%=params[:end_date]%> Please make sure chosen dates are correct. </p>
      <p> On Clicking you would be taken to dashboard page. </p>
      <p> You would receive e-mail between 3 to 10 minutes based on size of data and internet speed. </p>


        <%= link_to 'Send backup in e-mail', :controller => 'backup', :action => :backup_data, :start_date => params[:start_date], :end_date => params[:end_date]%>

    <% end %>

</div>

<script>
    $('#start_date').datepicker({format: 'dd-mm-yyyy', autoclose: true, todayHighlight: true, clearBtn: true})
    $('#end_date').datepicker({format: 'dd-mm-yyyy', autoclose: true, todayHighlight: true, clearBtn: true})
</script>
