<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <% if user_signed_in? %>
    <i style="color: #ffffff;font-size: 12px; text-align: left"><%=current_user.email%></i>
  <% end %>

  <div class="col-md-offset-2 col-md-8">
    <ul class="nav navbar-nav">
      <li>
        <%= link_to(root_path) do %>
          <span class="glyphicon glyphicon-home"></span>
        <% end %>
      </li>
      <% if user_signed_in? %>
        <li><%= link_to 'Calendar', :controller => 'calendar', :action => :index %></li>
        <li><%= link_to 'Clients', clients_path %></li>
        <li><%= link_to 'Bank', :controller => 'bank', :action => :index %></li>
        <li><%= link_to 'IST', :controller => 'incoming_service_taxes', :action => :index %></li>

    <% end %>

    </ul>

    <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Tasks', tasks_path %></li>
        <li><%= link_to 'Activity', :controller => 'activity', :action => :index%></li>
        <li>
          <%= link_to(destroy_user_session_path, :method => :delete) do %>
            <span class="glyphicon glyphicon-log-out"></span>
          <% end %>
        </li>
      </ul>
    <% end %>
  </div>
</nav>