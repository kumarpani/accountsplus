<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="col-md-offset-2 col-md-8">

    <% if user_signed_in? %>

            <ul class="nav navbar-nav">
                <li>
                <%= link_to(root_path) do %><span class="glyphicon glyphicon-home"></span>
                  <% end %>
                </li>
                <li><%= link_to 'Calendar', :controller => 'calendar', :action => :index %></li>
                <li><%= link_to 'Clients', clients_path %></li>
                <li><%= link_to 'Taxes', :controller => 'taxes', :action => :index%></li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
              <% if current_user.is_admin? %>
                  <!--<li><%= link_to 'Backup', :controller => 'backup', :action => :index%></li>-->
              <%end%>
                <li><%= link_to 'Activity', :controller => 'activity', :action => :index%></li>
                <li>
                  <%= link_to(destroy_user_session_path, :method => :delete) do %><span class="glyphicon glyphicon-log-out"></span>
                  <% end %>
                </li>
            </ul>
        <% end %>
  </div>
</nav>